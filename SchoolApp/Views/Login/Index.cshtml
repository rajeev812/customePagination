
@{
    ViewBag.Title = "Login";
}
@model SchoolApp.Entity.User
<section class="main" style="padding-top: 12%;">
    @if (ViewBag.ErrorMessage != null)
            {
        <div class="alert alert-danger" role="alert">
            <button class="close" data-dismiss="alert" type="button">
                <span aria-hidden="true">×</span>
                <span class="sr-only">Close</span>
            </button>
            <strong>Opps! </strong>
            @ViewBag.ErrorMessage
        </div>
    }
    @*@using (Html.BeginForm("Index", "Login", null, FormMethod.Post, new { @class = "form-3" }))
    {
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.AntiForgeryToken()
        <p class="clearfix">

            <label for="login">Username</label>
            @Html.TextBoxFor(m => m.UserName, new { @placeholder = "User Name" })
            <input type="text" name="UserName" id="UserName" placeholder="Username">
        </p>
        <p class="clearfix">
            <label for="password">Password</label>
            @Html.PasswordFor(m => m.Password, new { @placeholder = "Password" })
            <input type="password" name="Password" id="Password" placeholder="Password">
        </p>
        <p class="clearfix">
            <input type="checkbox" name="remember" id="remember1">
            <label for="remember">Remember me</label>
        </p>
        <p class="clearfix">
            @*<input type="button" name="submit" value="Sign in" class="hmbut"></input>
            <input type="submit" value="Sign In" class="hmbut" />
        </p>
    }*@
    @using (Html.BeginForm("Index", "Login", null, FormMethod.Post, new { @class = "form-3" }))
    {
            <p class="clearfix">
                <label for="login">Username</label>
                <input type="text" name="UserName" id="UserName" placeholder="User Name">
            </p>
            <p class="clearfix">
                <label for="password">Password</label>
                <input type="password" name="password" id="password" placeholder="Password">
            </p>
            <p class="clearfix">
                <input type="checkbox" name="remember" id="remember1">
                <label for="remember">Remember me</label>
            </p>
            <p class="clearfix">
                @*<a href="dashboard.html">*@
                    <input type="submit" name="submit" value="Sign in" class="hmbut"/>
                @*</a>*@
            </p>
    }
</section>
